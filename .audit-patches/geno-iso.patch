From 068fa4a69ab5e6661422ce6d0ee67042d9cd14dc Mon Sep 17 00:00:00 2001
From: Claude <noreply@anthropic.com>
Date: Sat, 30 May 2026 12:11:14 +0000
Subject: [PATCH] chore(geno-audit): bring geno-iso into ecosystem compliance

- Add versioning convention to GENO.md Conventions section
- Remove /gt- alias examples from command prefix aliasing (use canonical /geno- only)
- Refactor "Adding a new skill" to be repo-specific (not ecosystem-wide process restatement)
- Register geno-iso-housekeep skill in package.json skills map
- Add geno-iso-housekeep to GENO.md skills table and repo structure
- Add allowed-tools to geno-iso-housekeep/SKILL.md frontmatter

https://claude.ai/code/session_01Y1qoFoZiNFdcH3jMGxTTgS
---
 GENO.md                            | 17 ++++++++++++-----
 package.json                       |  3 ++-
 skills/geno-iso-housekeep/SKILL.md |  1 +
 3 files changed, 15 insertions(+), 6 deletions(-)

diff --git a/GENO.md b/GENO.md
index ee2a51a..28af08c 100644
--- a/GENO.md
+++ b/GENO.md
@@ -13,6 +13,7 @@
 | geno-iso-images-build | images | /geno-iso-images-build |
 | geno-iso-credentials-extract | credentials | /geno-iso-credentials-extract |
 | geno-iso-dev-guide | -- | /geno-iso-dev-guide |
+| geno-iso-housekeep | -- | /geno-iso-housekeep |
 
 ## Repo structure
 
@@ -40,7 +41,8 @@ geno-iso/
 │   ├── geno-iso-containers-enter/     # interactive enter
 │   ├── geno-iso-images-build/         # build Docker image
 │   ├── geno-iso-credentials-extract/  # refresh credentials
-│   └── geno-iso-dev-guide/            # development guide
+│   ├── geno-iso-dev-guide/            # development guide
+│   └── geno-iso-housekeep/            # background housekeeping daemon
 └── docs/                # MkDocs Material site
 ```
 
@@ -48,15 +50,20 @@ geno-iso/
 
 ### Command prefix aliasing
 
-Skills in this repo use the canonical `geno-` prefix in source (e.g., `geno-iso-containers-run`). When installed via `geno-tools`, the installer may configure shorter `/gt-` aliases (e.g., `/gt-iso-containers-run`) depending on the user's settings. Always author skill names and docs with the canonical `geno-` prefix; alias mapping is handled at install time and never committed to this repo.
+Skills in this repo always use the canonical `geno-` prefix in source (e.g., `/geno-iso-containers-run`). User-configured prefix aliases are applied at install time by `geno-tools install` and are never committed to this repo.
 
-### Adding a new skill
+### Versioning
+
+The canonical version lives in `genotools.yaml` (`version` field). The same value must appear in `pyproject.toml` (`project.version`), `package.json` (`version`), and `geno_iso/__init__.py` (`__version__`). Bump all four files together whenever skills are added, removed, or behavior changes.
+
+### Adding a new skill to this repo
 
 1. Create a directory under `skills/` named after the skill (e.g., `skills/geno-iso-<verb>-<noun>/`).
-2. Add a `SKILL.md` with YAML front matter (`name`, `description`, `allowed-tools`, `argument-hint`, `license`, `metadata`).
+2. Add a `SKILL.md` inside it (see the ecosystem skill authoring guide for required frontmatter fields).
 3. Register the skill in `package.json` under the `skills` map.
-4. Add a row to the Skills table in this file.
+4. Add a row to the Skills table in this file (GENO.md).
 5. If the skill belongs to a sub-skillset, note it in the table's Sub-skillset column.
+6. Bump the version in all four files listed above.
 
 ## CLI
 
diff --git a/package.json b/package.json
index c6534ae..0ff5fa6 100644
--- a/package.json
+++ b/package.json
@@ -9,7 +9,8 @@
     "geno-iso-containers-enter": "skills/geno-iso-containers-enter",
     "geno-iso-images-build": "skills/geno-iso-images-build",
     "geno-iso-credentials-extract": "skills/geno-iso-credentials-extract",
-    "geno-iso-dev-guide": "skills/geno-iso-dev-guide"
+    "geno-iso-dev-guide": "skills/geno-iso-dev-guide",
+    "geno-iso-housekeep": "skills/geno-iso-housekeep"
   },
   "repository": {
     "type": "git",
diff --git a/skills/geno-iso-housekeep/SKILL.md b/skills/geno-iso-housekeep/SKILL.md
index 11eba1e..32a20e9 100644
--- a/skills/geno-iso-housekeep/SKILL.md
+++ b/skills/geno-iso-housekeep/SKILL.md
@@ -4,6 +4,7 @@ description: >-
   Background housekeeping daemon — wiki maintenance, session mining, health
   aggregation, retro triage, and discovery scanning. Runs autonomously via
   /loop in a geno-iso container. Use when user says /geno-iso-housekeep.
+allowed-tools: "Bash(geno-trace *) Bash(geno-notes *) Bash(geno-mine *) Bash(geno-discover *)"
 argument-hint: "[--once] [--task <specific-task>]"
 license: MIT
 metadata:
-- 
2.43.0

